export enum tagTypes {
    user = "user",
    services = "services",
    admin = "admin",
    booking = "booking",
}

export const tagTypesList = [
    tagTypes.user,
    tagTypes.services,
    tagTypes.admin,
    tagTypes.booking,
];
